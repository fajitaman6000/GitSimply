# main.py
from app import GitVibeApp

if __name__ == "__main__":
    # Before running, make sure Git is installed on the system
    # and accessible from the command line.
    app = GitVibeApp()
    app.mainloop()